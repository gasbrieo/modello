import Typography from "@/components/Typography";

import "./Workspace.scss";

const Workspace = () => {
  return (
    <section className="workspace">
      <Typography variant="h6">Meu workspace</Typography>
    </section>
  );
};

export default Workspace;
